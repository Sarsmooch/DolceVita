<section class="section" id="{{ $id }}">
    <div class="container">
        <div class="section-header">
            <h3 class="section-title">{{ $title }}</h3>
            <p class="section-description text-secondary">{{ $description }}</p>
        </div>
        <div class="section-content">
            {{ $content }}
        </div>
    </div>
</section>
